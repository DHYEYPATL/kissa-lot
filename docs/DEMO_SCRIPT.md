# 3-minute demo script (English)

Record the running app. This is a product demo, not a trailer. Keep it under 3:00. Upload to YouTube or Vimeo, public.

0:00–0:20  
Screen: Kissa Lot UI. You: "Indie films die in prep. Crowds already said what they want. Kissa Lot is a Gemini agent that uses Parallel Search to package a story before you lock pages."

0:20–0:40  
Paste `examples/night_kitchen.fountain`. Point at INT/EXT pages. Hit **Run the desk**.

0:40–1:20  
While it runs, cut to a terminal or a brief code peek:
- `from parallel import Parallel` / `client.search(...)`
- `from google import genai`
- ADK `root_agent` tools  
Say: "Four live searches — audience, market, authenticity, production. Then Gemini writes the packet and may only cite those URLs."

1:20–2:20  
Back on the UI. Read the verdict, complexity score, one audience-desire paragraph, one cut, one clip moment, one citation. Click a Parallel-sourced link so the judge sees a real URL.

2:20–2:50  
"This is the Parallel track: runtime Search API, not a README mention. The workflow is development — filmmakers and junior execs — not a chatbot."

2:50–3:00  
Repo URL on screen. End.
